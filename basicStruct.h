#ifndef BASICSTRUCT_H
#define BASICSTRUCT_H


// Payload Message, together message_t struct is used to build a real packet.
typedef nx_struct PayloadMsg {
  // field
} PayloadMsg;

// Costants.
enum {
  AMPLITUDE_MODULATION = 6,
};

#endif